import { StyleSheet } from 'react-native';

export const recordScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    messageContainer: {
        alignItems: 'center',
    },
    message: {
        fontFamily: 'SCDream3',
        fontSize: 20,
    },
});
